#include<stdio.h>
void main()
{
int i,n,sbig=0,f=0;
printf("Enter the limit here:");
scanf("%d",&i);
printf("\n Enter the %d numbers \n",i);
while(i>0)
{
scanf("%d",&n);
if(n>f)
{
sbig=f;
f=n;
}
if(n>sbig && n<f)
{
sbig=n;
}
i=i-1;
}
printf("The second largest number is %d:",sbig);
}
