#include <stdio.h>
int main()
{
int num;
printf("Enter a number:");
scanf("%d",&num);
int count=0;
for(int i=2;i<num;i++)
{
if(num%i==0)
count++;
}
if(count!=0)
{
printf("It is not a prime number");
}
else
{
printf("It is a prime number");
}
return 0;
}
